# Dataset

Add the historical name dataset here as `name_trends.csv` with at least these columns:

- `Year`
- `Name`
- `Count`

A `Gender` column may also be included for gender-based analysis. Do not commit private or restricted datasets.
